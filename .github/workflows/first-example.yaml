name: My first workkflow

on: push

jobs:
    first_job: 
       runs-on: ubuntu-latest
       steps: 
          - name: Checkout Repo
            uses: actions/checkout@v5

          - name: List and read file
            run: | 
                echo "Im the best in github actions "
                ls -ltra
                cat README.md
            
        #   - name: List files
        #     run: ls

        #   - name: Read file 
        #     run: cat README.md